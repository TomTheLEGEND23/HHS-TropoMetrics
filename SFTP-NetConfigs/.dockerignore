# Ignore documentation and git files
README.md
.git
.gitignore

# Ignore Docker-related files in the image
Dockerfile
.dockerignore

# SSH host keys (mounted from K8s secret, not copied into image)
sftp_host_key
sftp_host_key.pub

# Only include the NetConfigs folder contents
!NetConfigs/
