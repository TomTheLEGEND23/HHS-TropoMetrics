# TropoMetrics Environment Variables

# Email API Configuration (Backend Service)
# These credentials are used by the email-api backend container
# They are NEVER exposed to the client/browser

# Gmail SMTP Server Configuration
EMAIL_SERVER=smtp.gmail.com:587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password

#Container Tag
TAG=dev

# Instructions:
# 1. Replace EMAIL_USERNAME with your Gmail address
# 2. Generate a Gmail App Password:
#    - Go to: https://myaccount.google.com/apppasswords
#    - Enable 2-Step Verification if not already enabled
#    - Generate an app password for "Mail" 
#    - Replace EMAIL_PASSWORD with the 16-character app password
# 3. Copy this file to .env: cp .env.example .env
# 4. Update the values in .env with your actual credentials
#
# Security:
# - These credentials are stored ONLY on the backend email-api container
# - The frontend never sees these credentials
# - All emails are sent through the secure backend API
