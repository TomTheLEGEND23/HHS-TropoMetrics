name: Build and Deploy Container

on:
  push:
    branches: [ main, dev, feature-Email-backend ]
  pull_request:
    branches: [ main, dev, feature-Email-backend ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata (tags, labels) for Docker
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha
          type=raw,value=latest,enable={{is_default_branch}}
          type=raw,value=main,enable=${{ github.ref == 'refs/heads/main' }}
          type=raw,value=dev,enable=${{ github.ref == 'refs/heads/dev' }}

    - name: Build and push Frontend Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./frontend
        file: ./frontend/Dockerfile
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}

    - name: Extract metadata for Email API
      id: meta-email
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-email-api
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha
          type=raw,value=latest,enable={{is_default_branch}}
          type=raw,value=main,enable=${{ github.ref == 'refs/heads/main' }}
          type=raw,value=dev,enable=${{ github.ref == 'refs/heads/dev' }}

    - name: Build and push Email API Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./email-api
        file: ./email-api/Dockerfile
        push: true
        tags: ${{ steps.meta-email.outputs.tags }}
        labels: ${{ steps.meta-email.outputs.labels }}

    - name: Set environment info
      id: env-info
      run: |
        BRANCH_NAME=${GITHUB_REF#refs/heads/}
        case $BRANCH_NAME in
          main)
            echo "environment=Production" >> $GITHUB_OUTPUT
            echo "namespace=tropometrics" >> $GITHUB_OUTPUT
            echo "port=30080" >> $GITHUB_OUTPUT
            ;;
          dev)
            echo "environment=Development" >> $GITHUB_OUTPUT
            echo "namespace=tropometrics" >> $GITHUB_OUTPUT
            echo "port=30081" >> $GITHUB_OUTPUT
            ;;
          *)
            echo "environment=Feature" >> $GITHUB_OUTPUT
            echo "namespace=tropometrics" >> $GITHUB_OUTPUT
            echo "port=30082" >> $GITHUB_OUTPUT
            ;;
        esac
        echo "branch=$BRANCH_NAME" >> $GITHUB_OUTPUT

    - name: Update manifest timestamp (Production)
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: |
        TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
        sed -i "s|kubectl.kubernetes.io/restartedAt:.*|kubectl.kubernetes.io/restartedAt: \"$TIMESTAMP\"|g" k8s/main-env.yaml
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add k8s/main-env.yaml
        if git diff-index --quiet HEAD; then
          echo "No changes to commit"
        else
          git commit -m "🔄 Update deployment timestamp [skip ci]"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main
        fi

    - name: Update manifest timestamp (Development)
      if: github.event_name == 'push' && github.ref == 'refs/heads/dev'
      run: |
        TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
        sed -i "s|kubectl.kubernetes.io/restartedAt:.*|kubectl.kubernetes.io/restartedAt: \"$TIMESTAMP\"|g" k8s/dev-env.yaml
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add k8s/dev-env.yaml
        if git diff-index --quiet HEAD; then
          echo "No changes to commit"
        else
          git commit -m "🔄 Update deployment timestamp [skip ci]"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:dev
        fi

    - name: Deployment notification
      if: github.event_name == 'push'
      run: |
        echo "✅ Docker image built and pushed successfully!"
        echo "🏷️  Branch: ${{ steps.env-info.outputs.branch }}"
        echo "🌍 Environment: ${{ steps.env-info.outputs.environment }}"
        echo "🐳 Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.env-info.outputs.branch }}"
        echo "📦 Namespace: ${{ steps.env-info.outputs.namespace }}"